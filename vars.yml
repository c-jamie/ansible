unix_home: "{{ lookup('env', 'HOME') }}"
unix_user: "{{ lookup('env', 'USER') }}"
m3: "{{ home }}/miniconda3/bin"
tools: "{{ home }}/tools"
nvmconfig: "{{ home }}/.config/nvm"
win_temp: "{{ lookup('env', 'Temp') }}"
github_repo_owner: "neovim"
github_repo_name: "neovim"
neovim_install_dir: "{tools}"